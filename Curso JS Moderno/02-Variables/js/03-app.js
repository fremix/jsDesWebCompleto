/* Variable const
Variable const: la primera diferencia es que no se pueden reasignar
Las variables const se deben inicializar

*/

const producto = 'tablet';

//No se pueden reasignar

// producto = 'monitor';

console.log(producto);

// se tiene que inicializar de esta forma
//no se puede reasignare l valor despues
const precio = 50; 
console.log(precio);